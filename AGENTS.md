# AGENTS.md

- Repo: UT Austin GeoAI slide deck built with Next.js + MDX + Theme UI.
- Main deck: `pages/index.mdx`.
- Slides use `---`; every deck exports `meta.title`.
- Commands: `make install`, `make serve`, `make check`, `make build`, `make clean`.
- Before handoff, run `make check`; run `bun run build` if export/deploy changed.
- Keep edits small; do not overwrite other uncommitted work.
